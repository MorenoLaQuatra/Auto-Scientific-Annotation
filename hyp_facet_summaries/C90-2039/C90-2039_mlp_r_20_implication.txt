5. node2 = Dereferencelnode2). Return(outnode). 5, the subgraphs of the result DG surrounded by the dashed rectangle can be shared with subgraphs of input structures G1 and G2, Section 4 proposes a method t.hat avoids this problem, Wroblewski's method first treats arcs with labels that exist in both input nodes and then treats arcs with unique labels. 5 due to a change of node Y G3/<a c g>). ELSE IF NotEmpty?(newarcs = CopyArcs(node)) THEN newnode = Create(node.typesymbol). Return(Nil_). Moreover, it is possible for each type symbol to select whether to apply feature unification order sorting or not. 1.