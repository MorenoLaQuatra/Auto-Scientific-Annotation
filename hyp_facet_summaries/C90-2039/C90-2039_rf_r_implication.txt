Strategic Lazy Incremental Copy Graph Unification The strategic lazy incremental copy graph unification method is a combination of two methods for unifying hmture structures. lh'esenl, address: 9 11, Midori cho 3-theme, Musashinoshi, Tokyo 180, Japan. A FORWARD slot value represents an eternal relationship while a COPY slot value represents a temporary relationship. Otherwise, the procedure obtains the output node with the meet as its TYPESYMBOL. Next, the procedure treats arcs. The procedure assumes the existence of two procedures, namely, SharedArcs and ComplementArcs. ELSE IF NotEmpty?(newarcs = CopyArcs(node)) THEN newnode = Create(node.typesymbol). ELSE AddArc(newnode, arc.label, arc.value). ENDIF Return(newarcs). 1.