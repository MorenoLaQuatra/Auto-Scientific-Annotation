For example, a spoken Present. A better method would nfinimize the copying of sharable varts. The redundantly copied parts are relatively large when input graphs have few common feature paths. 6). ENDIF Returo(newnode). Return(Nil_). IF NotNIL?(newnode) THEN newarc = CreateArc(arc.label, newnode). This is because the type symbol of a 'rFS represents salient information on the whole TFS. Wroblewski